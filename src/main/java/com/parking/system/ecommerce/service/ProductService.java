package com.parking.system.ecommerce.service;

public class ProductService {
    //see product information against and product id,
    // we can pass a product category as a filter parameter
    // we can pass price as a filter parameter

    //see search results
        //finding products according to search query which are in stock
        //order by price

    //what happens when there is multiple variation of a product like color and size
        //we should define different product for each variation
        //but then how to categorize on one item card on search results
}
